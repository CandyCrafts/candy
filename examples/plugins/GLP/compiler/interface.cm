pub (
    enum TokenType (
        BlockStmt
        BlockDecl
    )
    
    let (
        filter: interface
        
        Tokens: method(tt: TokenType) -> filter

        decl: method(f: filter)
    )
)